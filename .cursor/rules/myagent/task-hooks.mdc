---
description: MyAgent 任务钩子系统 - 钩子生命周期、内置钩子类型、自定义钩子开发
---

# MyAgent 任务钩子系统

## 概述

任务钩子（Task Hooks）是一个可扩展的任务生命周期管理系统，允许在任务执行的关键节点插入自定义逻辑。

### 钩子类型

| 钩子类型 | 触发时机 | 用途 |
|----------|----------|------|
| **preExec** | 任务执行前 | 权限检查、任务修改、资源预分配 |
| **postExec** | 任务完成后 | 结果处理、资源清理、指标收集 |
| **onProgressingNotify** | 任务进行中 | 实时进度更新、日志记录 |

## 钩子生命周期

```
┌─────────────────┐
│  提交任务       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  preExec Hooks  │
│  (可中断任务)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  执行任务       │
│                 │
│  onProgressing  │
│  Notify Hooks   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  postExec Hooks │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  返回结果       │
└─────────────────┘
```

## TaskContext

### 接口定义

```typescript
export interface TaskContext {
  // 任务标识
  taskId: string;
  sessionId: string;
  task: string;

  // 执行状态
  status: 'pending'' | 'running' | 'completed' | 'failed';

  // 任务上下文数据（用于 ContextManager 等）
  context: any;

  // 执行元数据
  metadata: {
    createdAt: Date;
    updatedAt: Date;
    llmCalls: number;
    skillCalls: number;
    totalTokens: number;
    userId?: string;
  };

  // Motia 服务引用
  services: {
    streams: any;
    logger: any;
    emit: any;
  };
}
```

### 使用示例

```typescript
// 在钩子中使用 TaskContext
class MyHook implements TaskHook {
  async preExec(context: TaskContext): Promise<PreExecResult> {
    console.log('任务 ID:', context.taskId);
    console.log('会话 ID:', context.sessionId);
    console.log('任务内容:', context.task);

    // 使用 Motia 服务
    context.services.logger.info('任务开始执行', {
      taskId: context.taskId,
    });

    // 发送事件
    context.services.emit('task.started', {
      taskId: context.taskId,
    });
  }
}
```

## TaskHook 基类

### 接口定义

```typescript
export interface TaskHook {
  /**
   * 任务执行前钩子
   * @returns 可以返回 { stop: true } 来中止任务执行
   */
  preExec?(context: TaskContext): Promise<PreExecResult>;

  /**
   * 任务完成后钩子
   */
  postExec?(context: TaskContext): Promise<void>;

  /**
   * 任务进行中通知钩子
   */
  onProgressingNotify?(context: TaskContext): Promise<void>;

  /**
   * 钩子名称（用于日志和调试）
   */
  readonly name: string;
}

export type PreExecResult = void | {
  stop?: boolean;          . // 是否停止任务
  reason?: string;          // 停止原因
  modifiedTask?: string;     // 修改后的任务
};
```

### 自定义钩子示例

```typescript
import { TaskHook, TaskContext, PreExecResult } from '@/core/task/hooks';

/**
 * 任务审计钩子 - 记录所有任务执行
 */
class AuditHook implements TaskHook {
  readonly name = 'AuditHook';

  async preExec(context: TaskContext): Promise<PreExecResult> {
    // 记录任务开始
    await this.logEvent(context, 'TASK_STARTED');

    // 检查用户权限
    if (!await this.checkPermission(context)) {
      return {
        stop: true,
        reason: 'Permission denied',
      };
    }
  }

  async postExec(context: TaskContext): Promise<void> {
    // 记录任务完成
    await this.logEvent(context, 'TASK_COMPLETED');
  }

  private async checkPermission(context: TaskContext): Promise<boolean> {
    // 实现权限检查逻辑
    return true;
  }

  private async logEvent(context: TaskContext, eventType: string): Promise<void> {
    // 实现事件记录逻辑
  }
}
```

## 内置钩子

### 1. UserAllowHook

**位置**：`src/core/task/hooks/built-in/user-allow-hook.ts`

**功能**：检查用户是否有权限执行任务

```typescript
export class UserAllowHook implements TaskHook {
  readonly name = 'UserAllowHook';

  async preExec(context: TaskContext): Promise<PreExecResult> {
    const userId = context.metadata.userId;

    if (!userId) {
      return {
        stop: true,
        reason: 'User ID not provided',
      };
    }

    // 检查用户权限
    const allowed = await this.checkUserPermission(userId);

    if (!allowed) {
      return {
        stop: true,
        reason: 'User does not have permission to execute tasks',
      };
    }
  }

  private async checkUserPermission(userId: string): Promise<boolean> {
    // 实现权限检查
    return true;
  }
}
```

### 2. DefaultTaskHook

**位置**：`src/core/task/hooks/built-in/default-task-hook.ts`

**功能**：提供默认的任务处理逻辑

```typescript
export class DefaultTaskHook implements TaskHook {
  readonly name = 'DefaultTaskHook';

  async preExec(context: TaskContext): Promise<PreExecResult> {
    // 设置默认上下文
    if (!context.context) {
      context.context = {};
    }

    // 添加时间戳
    context.context.startTime = Date.now();
  }

  async postExec(context: TaskContext): Promise<void> {
    // 计算执行时间
    if (context.context.startTime) {
      context.context.executionTime =
        Date.now() - context.context.startTime;
    }
  }
}
```

### 3. MetricsHook

**位置**：`src/core/task/hooks/built-in/metrics-hook.ts`

**功能**：收集任务执行指标

```typescript
export class MetricsHook implements TaskHook {
  readonly name = 'MetricsHook';

  private metrics = {
    totalTasks: 0,
    completedTasks: 0,
    failedTasks: 0,
    totalExecutionTime: 0,
    llmCalls: 0,
    skillCalls: 0,
    totalTokens: 0,
  };

  async preExec(context: TaskContext): Promise<void> {
    this.metrics.totalTasks++;
  }

  async postExec(context: TaskContext): Promise<void> {
    if (context.status === 'completed') {
      this.metrics.completedTasks++;
    } else if (context.status === 'failed') {
      this.metrics.failedTasks++;
    }

    // 累积指标
    this.metrics.llmCalls += context.metadata.llmCalls;
    this.metrics.skillCalls += context.metadata.skillCalls;
    this.metrics.totalTokens += context.metadata.totalTokens;
  }

  getMetrics() {
    return { ...this.metrics };
  }

  resetMetrics() {
    this.metrics = {
      totalTasks: 0,
      completedTasks: 0,
      failedTasks: 0,
      totalExecutionTime: 0,
      llmCalls: 0,
      skillCalls: 0,
      totalTokens: 0,
    };
  }
}
```

### 4. ContextManagerHook

**位置**：`src/core/task/hooks/built-in/context-manager-hook.ts`

**功能**：管理任务上下文的生命周期

```typescript
export class ContextManagerHook implements TaskHook {
  readonly name = 'ContextManagerHook';

  private contexts = new Map<string, any>();

  async preExec(context: TaskContext): Promise<void> {
    // 初始化上下文
    if (!this.contexts.has(context.taskId)) {
      this.contexts.set(context.taskId, {
        variables: {},
        history: [],
        metadata: {},
      });
    }

    // 更新 context 引用
    context.context = this.contexts.get(context.taskId);
  }

  async postExec(context: TaskContext): Promise<void> {
    // 清理过期的上下文
    this.cleanupExpiredContexts();
  }

  private cleanupExpiredContexts(): void {
    // 实现清理逻辑
  }
}
```

## 钩子注册表

### 钩子注册

```typescript
import { TaskHookRegistry } from '@/core/task/hooks/registry';
import { UserAllowHook, MetricsHook } from '@/core/task/hooks/built-in';

// 获取注册表实例
const registry = TaskHookRegistry.getInstance();

// 注册内置钩子
registry.register(new UserAllowHook());
registry.register(new MetricsHook());

// 注册自定义钩子
registry.register(new AuditHook());
registry.register(new RateLimitHook());
```

### 钩子执行

```typescript
// 执行 preExec 钩子
const preExecResult = await registry.executePreExec(context);

// 检查是否需要停止
if (preExecResult?.stop) {
  console.log('任务被停止:', preExecResult.reason);
  return;
}

// 使用修改后的任务
const task = preExecResult?.modifiedTask ?? context.task;

// 执行任务
await executeTask(task, context);

// 执行 postExec 钩子
await registry.executePostExec(context);
```

## 自定义钩子开发

### 示例 1：速率限制钩子

```typescript
import { TaskHook, TaskContext } from '@/core/task/hooks';

export class RateLimitHook implements TaskHook {
  readonly name = 'RateLimitHook';

  private requests = new Map<string, number[]>();
  private readonly windowMs = 60000; // 1 分钟
  private readonly maxRequests = 10;   // 最多 10 次请求

  async preExec(context: TaskContext): Promise<PreExecResult> {
    const userId = context.metadata.userId || 'anonymous';
    const now = Date.now();

    // 获取用户请求记录
    let userRequests = this.requests.get(userId) || [];

    // 清理过期的请求
    userRequests = userRequests.filter(
      timestamp => now - timestamp < this.windowMs
    );

    // 检查是否超过限制
    if (userRequests.length >= this.maxRequests) {
      return {
        stop: true,
        reason: 'Rate limit exceeded',
      };
    }

    // 记录新请求
    userRequests.push(now);
    this.requests.set(userId, userRequests);
  }
}
```

### 示例 2：任务验证钩子

```typescript
import { TaskHook, TaskContext } from '@/core/task/hooks';

export class TaskValidationHook implements TaskHook {
  readonly name = 'TaskValidationHook';

  private readonly maxLength = 10000;
  private readonly forbiddenPatterns = [
    /<script[^>]*>.*?<\/script>/gi,  // 防止 XSS
    /(DROP|DELETE|TRUNCATE)\s+(TABLE|DATABASE)/gi, // 防止 SQL 注入
  ];

  async preExec(context: TaskContext): Promise<PreExecResult> {
    const task = context.task;

    // 检查长度
    if (task.length > this.maxLength) {
      return {
        stop: true,
        reason: `Task exceeds maximum length of ${this.maxLength} characters`,
      };
    }

    // 检查禁止的模式
    for (const pattern of this.forbiddenPatterns) {
      if (pattern.test(task)) {
        return {
          stop: true,
          reason: 'Task contains forbidden content',
        };
        }
    }
  }
}
```

### 示例 3：日志增强钩子

```typescript
import { TaskHook, TaskContext } from '@/core/task/hooks';

export class LogEnhancementHook implements TaskHook {
  readonly name = 'LogEnhancementHook';

  async preExec(context: TaskContext): Promise<void> {
    context.services.logger.info('Task execution started', {
      taskId: context.taskId,
      sessionId: context.sessionId,
      taskLength: context.task.length,
      timestamp: new Date().toISOString(),
    });
  }

  async postExec(context: TaskContext): Promise<void> {
    context.services.logger.info('Task execution completed', {
      taskId: context.taskId,
      status: context.status,
      llmCalls: context.metadata.llmCalls,
      skillCalls: context.metadata.skillCalls,
      totalTokens: context.metadata.totalTokens,
      executionTime:
        context.metadata.updatedAt.getTime() -
        context.metadata.createdAt.getTime(),
    });
  }

  async onProgressingNotify(context: TaskContext): Promise<void> {
    context.services.logger.debug('Task progress update', {
      taskId: context.taskId,
      status: context.status,
      llmCalls: context.metadata.llmCalls,
    });
  }
}
```

## 钩子执行顺序

钩子按照注册顺序执行，重要钩子应优先注册：

```typescript
const registry = TaskHookRegistry.getInstance();

// 按优先级注册
registry.register(new UserAllowHook());      // 1. 权限检查（最高优先级）
registry.register(new TaskValidationHook()); // 2. 任务验证
registry.register(new RateLimitHook());       // 3. 速率限制
registry.register(new DefaultTaskHook());     // 4. 默认处理
registry.register(new MetricsHook());        // 5. 指标收集
registry.register(new LogEnhancementHook()); // 6. 日志增强
```

## 钩子最佳实践

### 1. 钩子应快速失败

```typescript
// ✅ 好：快速验证
async preExec(context: TaskContext): Promise<PreExecResult> {
  if (!context.task) {
    return { stop: true, reason: 'Task is required' };
  }
}

// ❌ 不好：不必要的复杂检查
async preExec(context: TaskContext): Promise<PreExecResult> {
  // 检查可以简化或移到其他地方
  const result = await complexDatabaseQuery();
  if (result.invalid) {
    return { stop: true, reason: 'Invalid state' };
  }
}
```

### 2. 钩子应幂等

```typescript
// ✅ 好：幂等操作
async postExec(context: TaskContext): Promise<void> {
  // 使用 upsert 而不是 insert
  await this.db.taskLog.upsert({
    taskId: context.taskId,
    status: context.status,
  });
}
```

### 3. 钩子不应修改核心状态

```typescript
// ❌ 不好：直接修改 context
async preExec(context: TaskContext): Promise<void> {
  context.status = 'completed'; // 不应该这样
}

// ✅ 好：使用 modifiedTask
async preExec(context: TaskContext): Promise<PreExecResult> {
  return {
    modifiedTask: cleanTask(context.task),
  };
}
```

### 4. 钩子应该有清晰的错误消息

```typescript
// ✅ 好：清晰的错误消息
return {
  stop: true,
  reason: `Rate limit exceeded: ${this.maxRequests} requests per ${this.windowMs}ms`,
};

// ❌ 不好：模糊的错误消息
return {
  stop: true,
  reason: 'Error',
};
```

## 相关文档

- `project-overview.mdc` - 项目概览
- `architecture.mdc` - 架构设计
- `agent-system.mdc` - Agent 系统
- `state-management.mdc` - 状态管理
