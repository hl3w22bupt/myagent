# 勾股定理教学视频 - 快速使用指南

## 📺 效果预览

生成的视频包含以下内容：

### 场景 1: 标题 (0-3.75秒)
```
大标题："勾股定理"
背景：紫色渐变
动画：淡入 + 弹簧缩放
```

### 场景 2: 三角形 (3.75-9秒)
```
逐步绘制直角三角形：
- 垂直边 a（蓝色）
- 水平边 b（蓝色）
- 斜边 c（粉色）
- 直角标记
- 边长标注
```

### 场景 3: 公式 (9-12.75秒)
```
公式：a² + b² = c²
动画：缩放淡入
颜色：a,b 蓝色，c 粉色
```

### 场景 4: 总结 (12.75-15秒)
```
标题：勾股定理
说明：直角三角形两直角边的平方和等于斜边的平方
动画：淡入
```

---

## 🚀 快速开始

### 方法 1: 使用测试脚本（推荐）

```bash
# 运行测试脚本
python3 scripts/test-educational-video.py

# 视频生成在：
outputs/videos/test_pythagorean_001_video_1.mp4
```

### 方法 2: 自定义参数

```python
import asyncio
from skills.remotion_generator.handler import generate_video

async def main():
    result = await generate_video({
        "description": "生成一个勾股定理教学视频",
        "duration": 15,           # 15秒
        "fps": 30,               # 30帧/秒
        "resolution": "1920x1080", # Full HD
        "style": "educational",   # 教育模板
        "output_format": "mp4",
        "quality": "medium"
    })

    if result["success"]:
        print(f"✅ 成功: {result['video_path']}")

asyncio.run(main())
```

---

## 📝 支持的描述格式

教育模板会自动识别以下关键词：

### 中文描述
```
✅ "勾股定理教学视频"
✅ "三角形定理动画"
✅ "数学公式演示"
✅ "生成一个关于勾股定理的视频"
```

### 英文描述
```
✅ "Pythagorean theorem tutorial"
✅ "Educational video about triangles"
✅ "Math formula animation"
```

### 带标题的描述
```
✅ "开篇显示'勾股定理'" → 标题 = "勾股定理"
✅ "标题'三角形教学'"   → 标题 = "三角形教学"
```

---

## 🎨 可自定义参数

| 参数 | 默认值 | 说明 | 可选值 |
|------|--------|------|--------|
| `duration` | 10 | 时长(秒) | 5-60 |
| `fps` | 30 | 帧率 | 24, 30, 60 |
| `resolution` | 1920x1080 | 分辨率 | 1280x720, 1920x1080, 3840x2160 |
| `quality` | medium | 质量 | low, medium, high, ultra |
| `output_format` | mp4 | 格式 | mp4, webm, gif |

---

## 🔍 问题排查

### Q1: 视频时长不对？
A: 检查 `duration` 参数，确保传入秒数（不是帧数）

### Q2: 没有显示三角形？
A: 确保描述中包含"三角"、"triangle"等关键词

### Q3: 标题显示错误？
A: 使用引号指定标题，如：`"显示'勾股定理'"`

### Q4: 生成的视频在哪里？
A: 默认保存在 `outputs/videos/` 目录

---

## 📊 输出格式

```python
{
    "success": True,
    "video_path": "/path/to/video.mp4",
    "duration": 15,
    "resolution": "1920x1080",
    "fps": 30,
    "file_size": 755714,
    "metadata": {
        "title": "勾股定理",
        "style": "educational",
        "format": "mp4"
    }
}
```

---

## 🎯 下一步

### 尝试其他内容
```python
# 其他教学主题
await generate_video({
    "description": "生成一个关于圆的教学视频",
    "duration": 20
})
```

### 调整时长
```python
# 更长的视频（30秒）
await generate_video({
    "description": "勾股定理详细讲解",
    "duration": 30
})
```

### 更高质量
```python
# 4K 高清视频
await generate_video({
    "description": "勾股定理",
    "resolution": "3840x2160",
    "quality": "high"
})
```

---

## 📚 相关文档

- [完整改进文档](./REMOTION_EDUCATIONAL_VIDEO_IMPROVEMENTS.md)
- [Remotion 官方文档](https://www.remotion.dev/docs)
- [技能配置说明](./../skills/remotion-generator/README.md)

---

**提示**: 查看预览页面 `outputs/preview-educational-video.html` 了解详细信息
